# Universal Plugin

Plugin package placeholder for Claude Code and Codex.

Current Claude hook configuration invokes:

```bash
skill-meter hook ingest --platform claude
```

The CLI can ingest hook events, import queued JSONL into SQLite, and print summaries. Full plugin install/doctor automation and Codex hook packaging are still planned.
